# iv-option
# 📊 تحلیل یادگیری ماشین اختیار معامله AAPL

یک پروژه جامع یادگیری ماشین برای تحلیل داده‌های اختیار معامله اپل (AAPL) از سال 2016 تا 2023، شامل تکنیک‌های رگرسیون، طبقه‌بندی و خوشه‌بندی برای پیش‌بینی نوسانات ضمنی و دسته‌بندی انواع اختیار معامله.

## 🎯 معرفی پروژه

این پروژه سه نوع تحلیل اصلی را بر روی داده‌های اختیار معامله AAPL انجام می‌دهد:

1. **تحلیل رگرسیون**: پیش‌بینی نوسانات ضمنی (IV) روز بعد
2. **تحلیل طبقه‌بندی**: پیش‌بینی جهت تغییرات IV (صعودی/نزولی با آستانه ±2%)
3. **تحلیل خوشه‌بندی**: دسته‌بندی اختیار معامله‌ها با استفاده از الگوریتم K-Means

## 📥 دانلود داده‌ها

### 🔗 منبع داده

داده‌های این پروژه از Kaggle قابل دانلود است:

**لینک دانلود**: [AAPL Options Data 2016-2023](https://www.kaggle.com/datasets/kylegraupe/aapl-options-data-2016-2023)

### 📋 مراحل دانلود و آماده‌سازی

1. **ثبت‌نام در Kaggle** (در صورت نداشتن حساب کاربری):
   - به سایت [Kaggle.com](https://www.kaggle.com) بروید
   - یک حساب کاربری رایگان بسازید

2. **دانلود دیتاست**:
   - به [لینک دیتاست](https://www.kaggle.com/datasets/kylegraupe/aapl-options-data-2016-2023) بروید
   - روی دکمه "Download" کلیک کنید
   - فایل ZIP دانلود می‌شود

3. **استخراج فایل‌ها**:
```bash
   # استخراج فایل ZIP
   unzip aapl-options-data-2016-2023.zip -d datasets/

### مدل‌های یادگیری ماشین

#### مدل‌های رگرسیون (پیش‌بینی IV روز بعد):
- رگرسیون خطی (Linear Regression)
- نزدیک‌ترین همسایگان (K-Nearest Neighbors)
- درخت تصمیم (Decision Tree Regressor)
- جنگل تصادفی (Random Forest Regressor)
- شبکه عصبی چندلایه (Multi-Layer Perceptron)

#### مدل‌های طبقه‌بندی (پیش‌بینی جهت تغییرات IV):
- رگرسیون لجستیک (Logistic Regression)
- نزدیک‌ترین همسایگان (K-Nearest Neighbors)
- بیز ساده (Naive Bayes)
- درخت تصمیم (Decision Tree Classifier)
- جنگل تصادفی (Random Forest Classifier)
- شبکه عصبی چندلایه (Multi-Layer Perceptron)

#### خوشه‌بندی:
- K-Means (K=3)
- روش آرنج برای تعیین K بهینه

### معیارهای ارزیابی

**رگرسیون:**
- میانگین مربعات خطا (MSE)
- میانگین قدر مطلق خطا (MAE)
- ضریب تعیین (R²)

**طبقه‌بندی:**
- دقت (Accuracy)
- صحت (Precision)
- بازخوانی (Recall)
- امتیاز F1
- سطح زیر منحنی ROC (AUC)

**خوشه‌بندی:**
- امتیاز Silhouette
- اینرسی (مجموع مربعات درون خوشه‌ای)

## 📈 خروجی‌ها

اسکریپت شش فایل تصویری تولید می‌کند:

1. `regression_results.png` - مقایسه عملکرد مدل‌های رگرسیون
2. `feature_importance_regression.png` - اهمیت ویژگی‌ها برای رگرسیون
3. `classification_results.png` - مقایسه معیارهای طبقه‌بندی
4. `feature_importance_classification.png` - اهمیت ویژگی‌ها برای طبقه‌بندی
5. `clustering_results.png` - تصویرسازی خوشه‌بندی K-Means
6. `elbow_method.png` - منحنی آرنج برای انتخاب تعداد خوشه بهینه

## 📋 ساختار پروژه


aapl-options-ml-analysis/
│
├── datasets/
│   ├── AAPL_options.csv
│   └── AAPL_options_2.csv
│
├── aapl_options_analysis.py
├── requirements.txt
├── README.md
├── .gitignore
│
└── outputs/
├── regression_results.png
├── feature_importance_regression.png
├── classification_results.png
├── feature_importance_classification.png
├── clustering_results.png
└── elbow_method.png

## 🔍 نکات کلیدی

### مهندسی ویژگی

اسکریپت چندین ویژگی مشتق شده ایجاد می‌کند:

- **Moneyness**: نسبت Strike/Underlying برای سنجش ITM/OTM بودن اختیار معامله
- **Volume_log**: حجم معاملات لگاریتمی برای توزیع بهتر
- **Bid_Ask_Spread**: اختلاف بین قیمت خرید و فروش
- **Mid_Price**: میانگین قیمت خرید و فروش

### اعتبارسنجی سری زمانی

پروژه از تقسیم صحیح سری زمانی برای جلوگیری از نشت داده استفاده می‌کند:

- **آموزش**: داده‌های 2016-2020
- **تست**: داده‌های 2021-2023

این روش تضمین می‌کند که مدل روی داده‌های آینده و نادیده تست می‌شود.

## 🛠️ سفارشی‌سازی

### تنظیم آستانه طبقه‌بندی

برای تغییر آستانه جهت IV (پیش‌فرض ±2%):

python
# در بخش طبقه‌بندی، این خط را تغییر دهید:
train_data['Label'] = train_data['IV_change_pct'].apply(
lambda x: 1 if x > آستانه_شما else (0 if x < -آستانه_شما else 2)
)

### تغییر تعداد خوشه‌ها

برای استفاده از K متفاوت در K-Means:

python
kmeans = KMeans(n_clusters=تعداد_خوشه_شما, random_state=42, n_init=10)

### پارامترهای مدل

پارامترهای هر مدل را می‌توان در دیکشنری‌های مربوطه تنظیم کرد:

- `models_reg` برای مدل‌های رگرسیون
- `models_class` برای مدل‌های طبقه‌بندی

## 📝 نکات مهم

- اسکریپت اختیار معامله‌هایی با IV صفر یا منفی را حذف می‌کند
- فقط اختیار معامله‌های با نقدینگی (حجم > 0) شامل می‌شوند
- مقادیر NaN با دقت در کل فرآیند مدیریت می‌شوند
- تمام تصاویر به صورت خودکار ذخیره می‌شوند
- داده‌ها از Kaggle دانلود می‌شوند و در مخزن GitHub قرار نمی‌گیرند

## ⚠️ توجه

**این پروژه فقط برای اهداف آموزشی است و نباید به عنوان مشاوره مالی استفاده شود.**
